package net.foggies.trove.api.command;

import java.util.List;

public class CommandArgument extends IArgument {
    public CommandArgument(int index, List<String> options) {
        super(index, options);
    }
}
